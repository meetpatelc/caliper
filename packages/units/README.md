# Instrument units

Canonical SI unit inventory and `convertQuantity()` for [Instrument](https://github.com/meetpatelc/instrument).

This package lives in the Instrument repo (`packages/units`). It is not a second product.
