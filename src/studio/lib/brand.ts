import { WORKSHOP_STORAGE_KEY } from "@/lib/storage-keys";

// Was "gauge-workshop" — a product that no longer exists, holding every draft.
export const WORKSHOP_KEY = WORKSHOP_STORAGE_KEY.name;

export { domains, type EngineeringDomain as DomainId } from "@/lib/platform";
